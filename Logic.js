import React from "react";

/*
    Do work with qrcodes here:
    qrcode.success = false; <-- success or failure
	qrcode.icon = "check";  <-- https://icons.expo.fyi/ & filter: materialicons
*/
export const executeAlgorithm = (qrcodes) => {
    console.log(qrcodes);
    return qrcodes;
}